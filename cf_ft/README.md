# cf_ft/ — live ChangeFormer + SECOND semantic package

Imported by the running demo:

- `demo/tools.py` / `demo/pipeline.py` — `team_second` and `second_semantic`
- `registry.py` — checkpoint registry helper
- Checkpoints live under `gates/_cache/cf_ft/` (`best_by_val.pt`, `best_semantic.pt`)
